import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function LayoutMain({ children }) {
    return <View style={{ width: 375, height: "auto" }}></View>;
}

const styles = StyleSheet.create({});
