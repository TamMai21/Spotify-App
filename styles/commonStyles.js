import { StyleSheet } from "react-native";

export default StyleSheet.create({
    errorMessage: {
        color: "red",
        fontSize: 14,
        fontWeight: "bold",
    },
    // Add more styles as needed
});
